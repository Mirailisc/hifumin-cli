name: SayGex

on:
  workflow_dispatch:

jobs:
  bruh:
    name: Most important CI
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v3
        with:
          fetch-depth: 0

      - name: Gay
        uses: CunnyDev/177013-commits@main
        with:
          git-name: Mirailisc
          git-email: mirailisclm@gmail.com
